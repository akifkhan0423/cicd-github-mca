name: CI Pipeline

on:
    push:
        branches:
        - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v5
        - name: Display Message
        run: echo "Building application"
        -name:Run Tests
        run: echo "Testing Successfull"
        -name:Deploy
        run:echo "Deployment Successfull"
       